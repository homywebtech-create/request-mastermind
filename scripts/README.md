# 🚀 نظام إدارة الإصدارات التلقائي

## 📋 نظرة عامة

نظام تلقائي لزيادة رقم الإصدار (`versionCode`) واسم الإصدار (`versionName`) في تطبيق Android.

## 🎯 الميزات

- ✅ زيادة تلقائية لـ `versionCode`
- ✅ تحديث ذكي لـ `versionName` (Semantic Versioning)
- ✅ تحديث تلقائي لملف `android/app/build.gradle`
- ✅ تتبع تاريخ التحديثات في `version.json`
- ✅ أوامر سهلة الاستخدام

## 📦 كيفية الاستخدام

### 1️⃣ Patch (إصلاحات بسيطة)
زيادة الرقم الأخير: `1.3.3` → `1.3.4`

```bash
npm run bump:patch
```

### 2️⃣ Minor (ميزات جديدة)
زيادة الرقم الأوسط: `1.3.4` → `1.4.0`

```bash
npm run bump:minor
```

### 3️⃣ Major (تغييرات كبيرة)
زيادة الرقم الأول: `1.4.0` → `2.0.0`

```bash
npm run bump:major
```

## 🔄 سير العمل الكامل

### الطريقة التقليدية ❌
```bash
# 1. تحرير android/app/build.gradle يدوياً
# 2. تغيير versionCode من 28 إلى 29
# 3. تغيير versionName من "1.3.3" إلى "1.3.4"
# 4. حفظ الملف
```

### الطريقة التلقائية ✅
```bash
# خطوة واحدة فقط!
npm run bump:patch

# ثم المزامنة
npx cap sync android
```

## 📊 أمثلة على الإصدارات

| النوع | الاستخدام | المثال |
|------|-----------|--------|
| **Patch** | إصلاح أخطاء صغيرة | `1.3.3` → `1.3.4` |
| **Minor** | إضافة ميزات جديدة | `1.3.4` → `1.4.0` |
| **Major** | تغييرات كبيرة في التطبيق | `1.4.0` → `2.0.0` |

## 📁 الملفات

- `scripts/bump-version.js` - السكريبت الرئيسي
- `version.json` - ملف تتبع الإصدار الحالي
- `android/app/build.gradle` - يتم تحديثه تلقائياً

## 🎬 مثال على الاستخدام

```bash
# قبل التحديث
$ cat version.json
{
  "versionCode": 29,
  "versionName": "1.3.4"
}

# تشغيل التحديث
$ npm run bump:patch

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 نظام تحديث الإصدارات التلقائي
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 الإصدار الحالي:
   versionCode: 29
   versionName: 1.3.4

✨ الإصدار الجديد (patch):
   versionCode: 30 (+1)
   versionName: 1.3.5

📝 تحديث android/app/build.gradle...
✅ تم تحديث build.gradle بنجاح

💾 حفظ الإصدار الجديد...
✅ تم حفظ version.json بنجاح

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 تم تحديث الإصدار بنجاح!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 الخطوات التالية:
   1. npx cap sync android
   2. ابنِ التطبيق من Android Studio
   3. ارفع الإصدار الجديد

# بعد التحديث
$ cat version.json
{
  "versionCode": 30,
  "versionName": "1.3.5",
  "updatedAt": "2025-01-08T12:30:00.000Z",
  "bumpType": "patch"
}
```

## 🛠️ استكشاف الأخطاء

### المشكلة: "ملف version.json غير موجود"
✅ **الحل:** السكريبت سينشئه تلقائياً من القيم الحالية في `build.gradle`

### المشكلة: "خطأ في تحديث build.gradle"
✅ **الحل:** تأكد من وجود مجلد `android/app/` والملف `build.gradle`

## 💡 نصائح

1. **استخدم `patch`** للإصلاحات السريعة
2. **استخدم `minor`** للميزات الجديدة
3. **استخدم `major`** للتغييرات الكبيرة فقط
4. **دائماً نفذ** `npx cap sync android` بعد التحديث
5. **احفظ** `version.json` في Git للمزامنة مع الفريق

## 🔗 الأوامر المختصرة

```bash
# اختصارات سريعة
npm run bump:patch   # أكثر استخداماً ✨
npm run bump:minor   # للميزات الجديدة 🎁
npm run bump:major   # للتحديثات الكبرى 🚀
```
